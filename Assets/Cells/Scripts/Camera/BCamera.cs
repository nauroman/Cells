using UnityEngine;
using System.Collections;

public class BCamera : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
	
    }
	
    // Update is called once per frame
    void Update()
    {
//        transform.localRotation = new Vector3(20, transform.localRotation.y + 0.01f, 0);
//        transform.Rotate(new Vector3(0, 0.01f, 0));

        //   transform.RotateAround(Vector3.up, 0.001f);
    }
}
